{
  "name": "calc-nixfred-com",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "bun test tests/tokenops-math.test.js",
    "audit": "bun test tests/tokenops-math.test.js && playwright test"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}